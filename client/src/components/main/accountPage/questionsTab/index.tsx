/**
 * QuestionsTab component that displays the all questions asked by a given user.
 */
const QuestionsTab = (user: string | undefined) => (
  <div>
    {/* <div id='question_list' className='question_list'>
        {qlist.map((q, idx) => (
          <QuestionView q={q} key={idx} />
        ))}
      </div> */}
  </div>
);

export default QuestionsTab;
